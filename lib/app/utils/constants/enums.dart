enum OrderStatus {
  processing,
  shipped,
  pending,
  delivered,
}

enum OrderType {
  pickup,
  delivery,
}

enum PaymentType {
  paypal,
  googlePay,
  applePay,
  visa,
  paystack,
  masterCard,
}
